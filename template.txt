<html>
    <head>
        <title>{{ print nom.' '.prenom; }}</title>
    </head>
    <body>
        <h1>
            {{ 4*(integerValue+integ);
                print ' and ';
                integ!=integerValue+integ-integerValue; }}
        </h1>
        Cours: {{ for c in ('Maths', 'Info') do 
            for j in ('Test', 'Element') do
                print c.', '.' : '.j;
            endfor;
        endfor;
        }}
    </body>
</html>